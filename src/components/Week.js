import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Week = ({ week }) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Week;

const styles = StyleSheet.create({});
